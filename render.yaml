services:
  - type: web
    name: roulette-app
    runtime: node
    region: tokyo
    plan: free
    startCommand: cd backend && npm install && node server.js
    envVars:
      - key: NODE_ENV
        value: production
